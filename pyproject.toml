[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
hello = "cli:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
